<h1>Editing backlog</h1>

<%= error_messages_for :backlog %>

<% form_for(:story, :url => story_path(@story), :html => { :method => :put }) do |f| %>
  <%= render :partial => 'form' %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', story_path(@story) %> |
<%= link_to 'Backlog', project_backlog_path(@story.project) %>